/** Shared by the wrapped preview and the exact-facet reference exporter. */
export const FABRICATION_GEOMETRY_OPTIONS = {
  subdivisionsPerSpan: 2,
  meridianCount: 12,
  capSteps: 1,
  radiusOffsetInches: 1,
} as const;
